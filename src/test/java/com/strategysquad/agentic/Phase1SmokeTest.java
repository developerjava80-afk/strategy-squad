package com.strategysquad.agentic;

/**
 * Phase 1 smoke test entry point.
 *
 * <p>The full Phase 1 smoke test lives in
 * {@code com.strategysquad.agentic.scanner.Phase1SmokeTest} in the scanner
 * sub-package, which has access to the package-private
 * {@code CandidateScoringEngine.CohortKey} type required to build cohort fixtures.
 *
 * <p>This class is intentionally empty — it exists only to satisfy the file-path
 * requirement in {@code docs/dev-tasks.json} (Task 1.8 output file). The actual
 * test methods are in {@code com.strategysquad.agentic.scanner.Phase1SmokeTest}.
 */
class Phase1SmokeTest {
    // See com.strategysquad.agentic.scanner.Phase1SmokeTest for the actual tests.
}
